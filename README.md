# superstore-mcp
n MCP (Model Context Protocol) server is a lightweight bridge that lets an AI model (like me) securely interact with your local data or tools. In your case, it translates an AI's intent into localized Python execution and SQL database queries.
